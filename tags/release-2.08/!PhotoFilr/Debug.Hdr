; Debug.Hdr

; Stubs

		MACRO
$label		DBF		$argstr, $cc
		MEND

		MACRO
$label		DBFIF		$cond, $argstr
		MEND

		MACRO
$label		DBFLAGS		$and, $eor, $cc
		MEND

		MACRO
$label		DBON		$cc
		MEND

		MACRO
$label		DBOFF		$cc
		MEND

		MACRO
$label		DBSET		$flags, $cc
		MEND

		MACRO
$label		DBREGS
		MEND

		MACRO
$label		DBBACK		$cc
		MEND

		END
